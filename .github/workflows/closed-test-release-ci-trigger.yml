name: Fresh closed-test AAB checkpoint

on:
  push:
    branches:
      - ci/fresh-closed-test-aab-20260817
    paths:
      - .github/workflows/closed-test-release-ci-trigger.yml

permissions:
  contents: read

jobs:
  closed-test-release:
    uses: ./.github/workflows/closed-test-release-core.yml
    with:
      confirmation: CLOSED_TEST
    secrets: inherit
